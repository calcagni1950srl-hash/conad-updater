name: Conad Bassi e Fissi Category Probe

on:
  push:
    paths:
      - 'conad_bassifissi_category_probe.py'
      - '.github/workflows/conad_bassifissi_category_probe.yml'
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install Playwright
        run: |
          pip install playwright
          python -m playwright install --with-deps chromium
      - name: Probe category filter
        run: python conad_bassifissi_category_probe.py
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: conad-bassifissi-category-probe
          path: |
            conad_bassifissi_category_probe.json
            conad_bassifissi_category_probe.png
          if-no-files-found: warn
